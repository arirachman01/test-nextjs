generator client {
  provider = "prisma-client-js"
  output   = "../generated/client"
}

datasource db {
  provider = "mysql"
  // logic is handled in prisma.config.mjs
}

model User {
  id    Int    @id @default(autoincrement())
  email String @unique
}
